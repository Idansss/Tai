# Drop covers (optional)

Drop a cover image here to override a drop card/page cover:

    public/drops/<drop-slug>.jpg           (.jpg / .png / .webp)

If no file exists, the drop shows a representative piece from its collection —
so this folder is optional. Drop slugs are the drop URL ids (e.g. `night-market`).
